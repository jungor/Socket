#include "AgendaUI.h"
#include<cstring>

AgendaUI aui;

int main(int argc, char **argv) {
    if (argc != 2){
        printf("usage: ./client <ipaddress>\n");
        exit(1);
    }
    strcnpy(aui.ipsrc, argv[1], strlen(argv[1]));
    aui.OperationLoop();
    return 0;
}
